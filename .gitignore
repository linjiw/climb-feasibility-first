# heavy / environment / external
bank/
logs/
mjlab-1.6.0/
bridge/
outputs/
# refeas is released separately as its own repo
refeas/
__pycache__/
*.pyc
*.pt
*.onnx
# bulky raw run data: keep aggregated json/csv/md/png, drop per-world arrays & per-clip shards
reports/**/*.npz
reports/feasibility_all/json/
reports/feasibility_e3/json/
reports/clip44_gate.html

# local simulator products; retain only adjudicated CSV/JSON summaries
reports/**/training/

# bulky per-clip screen shards; their manifests and aggregate tables are versioned
reports/eligibility/screens/
reports/P_SIGN/screens/
reports/segments_tier800/full/
reports/patches/

# exact DFRP panels: retain compact records/sidecars, not per-frame screens/logs
reports/dfrp_v1_exact_panel/*/screens_full/
reports/dfrp_v1_exact_panel/*/logs/
