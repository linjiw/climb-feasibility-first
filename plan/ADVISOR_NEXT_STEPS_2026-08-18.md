# Advisor guidance received 2026-08-18 (after G0 closure and the negative G1)

Verbatim, in order of priority:

N1 — knee-contact inverse dynamics through the descent (CPU, today). The one open alternative source. ArticulationView batched ID exists; if the reference is dynamically infeasible with knee contact, N3's primary prediction changes and its pre-registration must condition on this — so it runs first.

N2 — relational atlas v2 (CPU, today). Add support features: k-NN distance and category mass (by duration) of each clip's neighbors in the training bank. Two tests, both from data on disk: atlas residuals should concentrate on low-support clips with #44 the extreme point; and intrinsic+support features should lift cross-policy transfer from 0.567. Pre-register the uncontaminated version for E3 — policies trained on the 800 bank, where support values actually change, are the first cross-bank test of whether the atlas is now a transferable object.

N3 — the coverage-causality experiment (pre-register, then small GPU in gap capacity). Augment tier_mixed100 with 8–16 kneel/crawl neighbors selected by atlas proximity from the validated bank. Arms by priority: uniform-on-augmented (2 seeds, the keystone); adaptive-on-augmented (1 seed — prediction: the attractor releases and collapse costs less, nailing the interaction); random-augmentation specificity control (1 seed or a fine-tune variant). Pre-registered endpoints: #44 ground-segment survival rises from 0.000 at stratified start offsets; matched-easy unchanged; adaptive's mass on #44 decays as its failure EMA falls.

N4 — E3 pre-registration addendum before launch. Sharpen H2b to support-moderation: per-clip Δ(grounded−uniform) at 800 correlates with support change from 100→800, with low-support clips identified in advance. Document the 800 bank's category composition as an analyzed variable. And mandate the stratified-start evaluation protocol everywhere — the "0.31 survival" artifact means every start-offset-averaged difficulty label, including the atlas's, carries this confound; fix it before E3 generates labels at scale. An LP arm is now cleanly motivated (learning-progress deprioritizes zero-progress attractors by construction — the sampler-side fix, versus composition as the data-side fix) and completes the taxonomy for one arm's cost; add it only if the window allows.

N5 — redefine F and re-run the six-clip design once (~20 min), labeled instrument calibration. Signed differences of replicate means, distributional distances, and survival/phase-onset statistics, with the new noise floor published from the stock-mjlab arm. This is method development, not a second bite at P1 — the folded |Δ| statistic was chaos-dominated exactly as the Lyapunov numbers show, and no Phase-2 fragility design exists until the instrument resolves signal below the floor.

N6 — PhysFrag reprioritized honestly. The program's motivating anomaly dissolved; it doesn't die, but it comes off the critical path. The LUCID-correlation remains its one surviving test — with the caveat that N is small (few policies, few motions) — and Featherstone/XPBD, SONIC, and differentiable contacts stay frozen exactly as v4 lists. Fragility earns its way back through that correlation or waits for the next cycle. The conformance harness itself is the durable asset: a validated dual-sim instrument at |Δqvel| ≤ 3e-5 that any future physics question inherits.

Park the motor-strength sign reversal as one paragraph and a hypothesis ("a stronger robot executes wrong actions harder"), not a thrust — it's a lead with a CI, and it belongs in the coverage story's discussion, not on the critical path. And hold the v4 line under the excitement of a tidy resolution: no bank expansion to 200/800 to "confirm" the coverage story either — N3's sixteen-clip augmentation is the confirmation, E3 is the scale test, and the paper — collapse, the non-floor, the unsupported attractor, grounded repair, composition as the causal fix — is now one coherent argument from mechanism to intervention.
