# Complete confirmation result and useful-practice research update

Evidence completed 6 September 2026; publication prepared just after midnight
EDT on 7 September. Unsealed result/publication addendum. Original scientific
settings, seals and runtime sources are unchanged.

The public page now leads with the completed **inconclusive** result: 12 training
runs, 48 held-out cells, three paired seeds. It shows every final hard/all-panel
R−U seed difference, the seed-level 95% t intervals, the failed non-regression
criterion and the exploratory learning curves. It does not claim a positive
curriculum result, an efficiency accelerator, harm, equivalence or exclusion of
a target-sized benefit. The old 22:17 allocation export remains dated and linked.

The argument is admission → allocation → independent control outcome → usable
execution. The abstract now reports actual policy outcomes. Method text states
the pre-cap 80/20 mixture, its scope, full-access temporal support and the limits
of model admissibility. Prior work links distinguish curriculum precedent,
motion curation and adaptive motion sampling without asserting normalization
novelty. Submission milestones and current official deadline/page constraints
appear in the plan; incomplete secondary studies remain prospective.

The next-stage record separates the successful two-reference continuous CPU
development smoke from the pending U/D/R continuous study. The frozen-policy
instrumentation pilot is complete, with stationarity/noise-floor evidence still
pending. All nine corrected CUDA development cells completed and individually
replay, but the aggregate exact original/unchanged CSV parity gate failed.
No parity tolerance changed and no full physical study is enabled.

## Evidence and verification

- `reports/relative_confirmation_results_2026-09-06/README.md`: full result,
  qualifications and original campaign/analysis/contract identities.
- The same directory contains the three-panel PNG/PDF, `learning_curves.csv`,
  `summary.json`, `efficiency_descriptive.json` and `integrity_verification.json`.
- Exact serialized replay preserves every numeric value. The original efficiency
  failure came solely from tuple/list representation of `seed_order`; its failed
  receipt is preserved. No frozen analyzer edit or scientific revision occurred.
- All 376 runtime files match, all 41 Phase-G and seven confirmation seal entries
  pass; all 48 CSV hashes and 134,400 episode rows verify. These rows are correlated
  evaluation conditions, not additional trained-policy replication.
- An additional archived FGAS seal attempt could not finish because the unrelated
  local payload `bank/tiers/tier_mixed100.txt` is absent. That manifest was not
  changed; current confirmation seals and inputs pass independently.
- Twenty-five targeted tests pass: 13 support/continuity, five exact serialized
  replay and seven optional-sensor graph checks. New isolated sources compile.
- Browser review: 24 desktop/mobile and light/dark page views, local links,
  image loading, overflow, script errors, keyboard interaction and no-JavaScript
  fallback. Screenshots and results are under
  `reports/confirmation_pages_2026-09-06/`. The main result figure and mobile
  homepage were visually inspected. `git diff --check` passes.

Publication uses the existing separate Pages checkout, preserving the dirty
research workspace and source-bound experiment trees. Only the reviewed site,
research addenda, result exports and verification artifacts are copied. Original
motion payloads, checkpoints and simulation traces are not published.
The final deployment receipt is recorded after checking the actual Pages build
and exact public file bytes.
