# Fable revision 4: submission decision

7 September 2026. User-directed adoption of `fable.md` and its HTML rendering.
Unsealed planning decision; no completed scientific contract is revised.

The paper's current claim set is reference--physics misalignment measurement,
model-relative screening, and an exact-support training interface with a completed
controlled **inconclusive** allocation result. H1 admission on/off under D is the
only additional experiment pursued before the evidence cutoff. Continuous
execution, physical sensitivity, stationary-noise experiments, practice branches,
sampler extensions, repair expansion and hardware work stop until September 16.
The existing public site, companion and flagship remain unchanged.

Current no-H1 title: **When Failure Is Not Difficulty: Screening
Reference--Physics Misalignment and Testing Adaptive Allocation on Exact Support
for Humanoid Motion Tracking**. H1 can change the title and add an admission
result only after its complete prospectively specified comparison exists.
No pending result supports the abstract or contribution list.

H1 will use exactly **five paired fresh training seeds**, selected and audited
before freezing ten training jobs and forty evaluation cells. There is no
conditional sixth seed. The original three-seed confirmation is not expanded.
The new primary decision uses the two-sided paired seed t interval: positive if
its lower endpoint exceeds zero, negative if its upper endpoint is below zero,
otherwise inconclusive. The +0.02 line is descriptive; the all-panel difference
is reported with its interval without an additional pass/fail guard. This is a
new H1 design, not a change to the completed confirmation.

The frozen H1 protocol must contain the rejected-support manipulation checks,
above-prior allocation attribution, all training before any held-out evaluation,
exact source/input/configuration bindings, one attempt per GPU job, sentinels,
the existing 14,000 MiB / at-most-60% resource gate, and the calendar kill rules.
GPU entrypoint smokes remain required and will be included in the prospective
execution sequence before full training. No GPU launch precedes that binding.

Interpretation corrections to the guidance: an interval spanning zero does not
establish indistinguishability, equivalence, absence of harm or a below-resolution
effect. A confidence-interval half-width is a precision calculation, not statistical
power. The repair result does not prove safe or harm-free deployment. Manuscript
language will retain these distinctions rather than copying stronger claims.

Manuscript work proceeds now: replace stale E4 text with the separate E4
`not_tested` disposition and completed U/A/R/D result; insert paired seeds and
learning curves; compress repair; update title, abstract, contributions,
limitations and AI assistance disclosure. The submission build must contain all
essential evidence within eight pages. H1 is removed from the submission if its
launch/complete-training cutoff fails; partial seeds do not become a result.

Worktrees retained: original research master at `1552c99`; H1, ICRA evidence and
signal-quality trees at detached `1552c99`; Pages publication tree at `ac62b53`
when this decision was recorded. H1 remains in its isolated worktree; all original
runtime hashes and sealed artifacts remain intact. Source control will preserve
completed research and guidance, with bulky local training products excluded.
