# Advisor guidance received 2026-08-18 (second message, after N1–N6)

Verbatim summary of the instructions (full text kept in the session transcript):

- G0→G1→N1 = the project operating as designed; resolution: an *infeasible reference* (a full second
  of descent, ~329 N unsupported, no contact available) inside a category the bank barely contains,
  family-wide (12 of 40). G0's elimination protocol (per-substep paired stepping, MuJoCo-C as third
  referee, shadow solver) is publishable methods material; errors #8–11 add a fourth exposure layer —
  the taxonomy now spans scheduler, model, harness, data. Newton's role: the *referee* (validated
  second implementation, batched ID/LP for the feasibility screen, the instrument behind N5).
- Conceptual spine: difficulty = **feasibility × support × intrinsic**, each with its measurement
  (contact LP, kNN support, atlas features) and its fix (repair/exclude, composition, curriculum/robustness).

Next steps, in order:
1. Environment-admits-the-skill check (minutes, before N3 is interpreted): stiff PD mocap-follow of
   the kneel/crawl phase through the live env, logging every termination channel and reward. Seal as
   an N3 analysis precondition; if the env does not admit the skill, fix and re-baseline first.
2. Feasibility screen rollout, deadline order: (a) 800 bank + holdout before E3 (launch gate);
   (b) free re-analysis of Exp-1/2 endpoints feasible-only vs all (robustness companion; measure);
   (c) full 10,822-clip prevalence in background with per-category breakdown → decides release +
   upstream note to the retargeting pipeline.
3. E3 analysis addendum (new seal, before launch): make support-moderation bidirectional and risky —
   diluted categories (ground 3.2 % → 0.65 %) predict *negative* per-clip Δ for their members; name them.
4. N7 — repair, pre-registered after N3 reads out, tiny: contact-restoring projection of #44's
   transitions + 2–3 family clips; predictions: descent becomes learnable in the augmented bank; the
   motor-strength sign reversal vanishes on the repaired clip.
5. Atlas v2.1 now: feasibility features (unsupported force × duration, airborne-frame fraction);
   pre-register predicted lift on within-bank fit and on the 0.567 transfer.
6. Sign reversal as a signature (exploratory, piggyback on N5 seed-1): does strength-hurts localise to
   airborne windows? If it generalises → rollout-only infeasibility detector.
7. Writing starts now: evidence-complete sections (collapse + non-floor, G0 elimination protocol, G1
   negative with anatomy, N1 screen); decomposition figure as the spine; single flagship. **Keep CLIMB
   CPU-only until September 15** (LUCID's last four ICRA weeks stay clean).
8. Frozen stays frozen; PhysFrag inherits the validated N5 instrument if it returns.
Caution: pre-list N3's null follow-ups now (exposure mass insufficient → grounded targeting of the
family; within-family start-phase curriculum; env constraint from step 1). Sixteen clips at ~14 % of
uniform mass is a plausible dose, not a guaranteed one.
Also asked: state a critical research finding and a promising research direction for humanoid
tracking and deployment.
