# TASK companion-review — adversarial pass on companion draft v0.2 (2026-08-20)

*Reviewer stance: hostile-but-fair. Standing attack list applied top to bottom.
Verdict codes: MAJOR (blocks submission) / minor / resolved-in-place.*

## Attack 1 — threshold provenance (the 10 % flag, the 6 cm gap, the ½-weight bound)

- 10 %: provenance documented in the sealed eval policy (first appearance in the N3 seal
  `af1b7c9f`, predates the stratified-endpoint policy `a93a87a0`). §5 cites it. **resolved**.
- 6 cm gap: sensitivity 3/6/10 cm asserted in §2 — but the artifact lives in job scratch
  (`/home/robotixx/.claude/jobs/.../n1_*_{0.06,0.10}.json`), which is *ephemeral* and outside the
  repo. **MAJOR-1: a cited artifact must be durable. Fix: re-run the gap-sensitivity comparison
  into `reports/N1_gap_sensitivity.json` (CPU, minutes) before submission, or delete the claim.**
- ½-weight bound: no sensitivity stated anywhere. **minor-1: add one sentence — the CNRS/descent
  cases sit at ≈ 1.0× weight, far above any reasonable bound; report the flag rate at 0.25× and
  0.75× weight in a footnote (one CPU pass over the saved brief JSONs is NOT possible — brief
  output bakes the 0.5 threshold in. Either recompute from full-mode output for a subsample, or
  soften to "bound sensitivity unquantified; flagged mass is concentrated near 1× weight".)**

## Attack 2 — single-robot / single-reward scope

§2 states output-on-target-robot scope; abstract says "for the target robot". But the prevalence
table's caption language ("22.8 % of clips are dynamically infeasible") drifts toward
robot-independent phrasing in §4's opening. **minor-2: prefix the §4 table title with
"AMASS→Unitree-G1 (this pipeline, this robot)" — fixed in place.** The G1-only scope must also
appear in the abstract's first sentence about prevalence — it does ("AMASS→Unitree-G1 bank").
**resolved** after minor-2.

## Attack 3 — version pinning of both engines

Header pin block present; §6 names all three stacks + the recert spec. Newton pin is a commit
hash with pip-version ambiguity (1.6.0.dev0 vs the GA "1.0" line) — the draft acknowledges this
in the recert spec but not in the note body. **minor-3: add half a sentence to §6: "(release
numbering of the Newton line differs from this checkout's metadata; the commit hash is the
pin)".**

## Attack 4 — numbers lacking artifact paths

Sweep of every numeral in v0.2: all carry paths except (a) "joint speeds ≤ 5.6 rad/s" (§1) —
path is `plan/N1_RESULT.md`, present in the neighbouring sentence but not adjacent; **resolved**
(acceptable proximity); (b) "6–11 points" eval depression (§5) — derivable from
`reports/N_atlas_v21.json` endpoints_2b (all-vs-infeasible per arm: 0.810→0.750, 0.780→0.705,
0.825→0.741 = 6.0/7.5/8.4 points... **MAJOR-2: "6–11" is WRONG. The correct all-minus-infeasible
gaps are 6.0/7.5/8.4 points; the feasible-minus-infeasible gaps are 8.4/10.6/11.8. The "6–11"
conflates the two ranges. Fix: state "flagged clips score 6.0–8.4 points below the all-clips
aggregate (8.4–11.8 below the feasible stratum)" — and propagate to the flagship §6 and the
preview page, which carry the same "6–11" phrasing.**

## Attack 5 — claims lacking status labels

Caption of Figure 1 and Figure 2 carry [measured]; §3's corroboration carries [exploratory] with
the two-case count; §5's transfer paragraph carries both the sealed ✓ and the sealed-null.
Abstract labels every clause. **resolved.**

## Attack 6 — pending 🕐 doing load-bearing work

P-SIGN appears in §3 with an explicit "does no load-bearing work" disclaimer; N7 only inside
recommendation 5 as future work; N3/E3 only in the closing block. The sign-reversal corroboration
in the *abstract* is labelled exploratory with its pre-registered test named. Borderline: the
abstract's "independently corroborated by a rollout-only signature" could be read as established.
**minor-4: abstract wording changed to "corroborated in two seed-sets by a rollout-only
signature (generality pre-registered, pending)".**

## Attack 7 — displacement of the withdrawn-verdict story

§6 keeps it, names the 40-point fork, the misreading, and the withdrawal, and points to the
taxonomy. It survived the compression. **resolved.**

## Additional findings

- **minor-5**: §3 "replicated across two IC-seed sets (r = 0.92)" — the r = 0.92 is the
  *instrument-wide* agreement across 36 (axis, clip) effects, not the sign-reversal's own
  replication statistic; rephrase: "replicated across two IC-seed sets (+15.0/+16.0 mm;
  instrument-wide agreement r = 0.92)". Fixed.
- **minor-6**: §4 "three orders of magnitude of spread" — 0.1 %→100 % is 3 orders; fine, keep.

## Disposition

| id | severity | status |
|---|---|---|
| MAJOR-1 gap-sensitivity artifact is ephemeral | major | **fixed same day**: re-run into `reports/N1_gap_sensitivity.json` (see addendum below) |
| MAJOR-2 "6–11 points" mis-stated | major | **fixed same day** in companion §5, flagship S6, docs page; RESULTS_LOG row corrected |
| minor-1 ½-weight sensitivity | minor | softened wording + flag-rate at 0.25/0.75 recomputed for the heldout subsample from full-mode outputs where they exist; else stated as unquantified |
| minor-2..5 | minor | fixed in place |

**Zero unresolved majors after same-day fixes → P0 DoD review criterion met, conditional on the
two fixes landing (verified below by re-read).**

## Addendum — fix verification (same day)

- MAJOR-1: `reports/N1_gap_sensitivity.json` written (brief screen at 3/6/10 cm for the attractor
  + control; §2 rewritten to state the *actual* sensitivity honestly — 3 cm over-flags the
  control at 42 % via the bank's clearance offset; 10 cm degenerates by granting airborne
  geometry as contact; 6 cm sits between the failure modes). Note this is a *stronger* honesty
  position than v0.2's "checked, robust" — the review improved the claim.
- MAJOR-2: "6–11 points" corrected to "6.0–8.4 below the all-clips aggregate (8.4–11.8 below the
  feasible stratum)" in companion §5, flagship S6, docs page, dataset-advisory draft;
  RESULTS_LOG carries the corrected row.
- minors 1–5: applied (verified by re-read of v0.2).

**Zero unresolved majors. P0 review criterion: met.**

## v0.3 delta (2026-08-20, post-review addition — needs one reviewer pass before submission)

§5b added (deployment implications): all quantities labeled measured/estimate/exploratory/
pending; artifact paths present (`reports/effort_sat_at_fall.json`); no pending result does
load-bearing work (P-SIGN cited as the *test* of the runtime-guard idea, not its support).
Open review item: none identified by self-check; flagged for the external reviewer regardless.

## v0.3 delta 2 (2026-08-21): §7 deliverables trio + economics paragraph; repair-census numbers
enter only via [pending 🕐] until the aggregation lands; the 48.8 % exposure figure and the
repair validation rows carry artifact paths. Self-check: no pending result load-bearing.
