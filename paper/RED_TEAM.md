# Red-team audit — "what exposure, harness, or data artifact could explain this instead?"

Pass scheduled Sept 5–12 (RESEARCH_PLAN_v5). Skeleton opened 2026-08-19 so checks can be logged as
they are done rather than reconstructed. Format: claim → alternative artifact explanations →
closing check (specific, with artifact path) → status.

Prior escapes that motivate this pass: the transfer gap was an observation bug (stale first obs);
the adaptive deficit was a sampler bug (non-floor); the hardest clip was a data bug (airborne
retarget); the "contact-event fork" was four harness bugs (#8–11); the "0.31 survival" was a
protocol bug (start-offset averaging).

| # | claim (§) | alternative explanations to attack | closing check | status |
|---|---|---|---|---|
| 1 | Adaptive collapses onto one clip (§3) | logging artifact in telemetry; per-seed coincidence; resample-time confound | raw sampler telemetry across 3 seeds shows same clip (`A7`); clip identity stable across arms; entropy series continuous | ✅ closed (A7, A5) |
| 2 | Uniform beats adaptive (§3) | eval-set composition favouring uniform; horizon truncation; auto-reset bias | 3/3 seeds, paired; strata (D1) preserve ordering; wrap-freeze protocol in `climb_eval.py` documented | ✅ (strata re-checked 08-18) |
| 3 | Non-floor derivation (§3) | misread upstream code; version drift | checked 2026-08-26: [mjlab #1153](https://github.com/mujocolab/mjlab/issues/1153) and [whole_body_tracking #73](https://github.com/HybridRobotics/whole_body_tracking/issues/73) remain open with the derivation/repro intact, no maintainer reply/assignee/linked PR; mjlab `main` still uses the additive count expression. Drafts claim only "filed", not acknowledged or fixed. | ✅ thread/code state checked; upstream resolution open |
| 4 | Grounded ≫ adaptive (§4) | ρ=0.1 floor acts as different *exploration*, not coverage; extra uniform data volume | entropy/top-1 telemetry shows floor binds only at collapse; §10 now states explicitly that normalise-then-mix changes exploration as well as coverage and that the ledger is diagnostic, not a mediation analysis | ✅ wording closed 2026-08-26 |
| 5 | Conformance closure (§5.1) | agreement by coincidence of seeds; absorbed-writes hiding real divergence | per-substep |Δq̇| ≤ 3e-5 at every substep (not endpoints); MuJoCo-C referee; two repeats; absorbed-write count logged (57–59) | ✅ (`S1_RESULT.md`) |
| 6 | G1 negative (§5.2) | δ too small; φ too coarse; floor rule too strict | N5 recalibration finds real 6–14 mm effects with the *same data* — instrument, not δ, was the limit; negative stands under both statistics | ✅ (`g1_v2_summary.json`) |
| 7 | N1 airborne verdict (§5.3) | ground-alignment offset placed the whole clip too high; gap 6 cm arbitrary; qdd differentiation noise | gap sensitivity 3/6/10 cm run 08-17 (`n1_*_0.06/0.10.json`); control clip supported at every frame under same pipeline; standing phase of #44 itself is supported (internal control) | ✅ |
| 8 | Prevalence 22.8 % (§6) | flight conflated with infeasibility; screen bug on specific sources | category caveat sealed in atlas-v2.1 prereg *before* prevalence read; per-source spread argues pipeline; **partly closed 2026-08-19**: an independently written re-implementation (different codebase, MJCF and μ) returns 0.14 % on a second 4,950-clip bank — a screen that conflated flight with infeasibility, or carried a systematic bug, could not return near-zero there while 111 of its clips exceed 10 % airborne frames; **still to do: manually inspect 5 flagged CNRS + 5 Transitions clips** | ⏳ |
| 9 | Feasibility transfer lift (§7) | three extra features lift any ridge; leakage via screen fit to difficulty | 200-draw random-feature permutation (p = 0.010–0.045); screen never sees difficulty labels | ✅ (`N_atlas_v21.json`) |
| 10 | Sign reversal (§9) | window definition post-hoc; two seeds share the harness; motor response is not feasibility-specific | P-SIGN uses reference-only windows and matched controls but **fails**: 7/12 family, 4/12 controls, 2/7 localised | ❌ closed as a negative result; detector/runtime-guard promotion rejected (`plan/P_SIGN_RESULT.md`) |
| 11 | Support-residual correlation (§7) | kNN distance ≈ outlier-ness ≈ intrinsic hardness (circularity) | §7 states the raw association as measured and explicitly reserves the bank-change test for sealed-pending E3; no causal support claim is made | ✅ wording closed 2026-08-26; experiment pending |
| 12 | P-TAX null (§6) | tax measure too coarse (binary >1 cm); population too small | bootstrap CIs are wide but sign is consistently negative; sealed rule respected; stated as null with CI | ✅ |
| 13 | Oracle precondition (§8.1) | playback bypasses physics → terminations trivially pass? | playback *teleports then steps* — terminations evaluated on post-physics state; PD-follow failure on easy clips shows the oracle's limitation is balance, not terminations | ✅ (`N3_PRECONDITION`) |

Open items after the 2026-08-26 writing pass: #8 (expand the existing 3-CNRS / 1-Transitions
hand-check to the planned 5 + 5), #14 (final bibliography pass), #20
(cross-implementation agreement on a shared sample; the durable second-bank CSV is now closed),
#21 (segment-curation rule choices — open by construction until a training arm consumes curated
segments), and #25 (adaptive allocation — open by construction until its manipulation gate passes).

| 14 | Related-work positioning (§2) | misattributed sampler lineage; invented refs | 12 citations live-verified 2026-08-20 (arXiv/DOI listed in RESULTS_LOG); ○-marked standards flagged for final bib pass; LUCID explicitly flagged internal | ⏳ final bib pass |
| 15 | "Screen catches what kinematic QC cannot" (§2, §6) | GMR/Retargeting-Matters criteria might already catch it | their criteria are clearance/sliding/self-intersection — the airborne descent has *good* clearance by construction; state as: complementary, dynamic vs kinematic | ✅ wording in S2 |
| 16 | Wasted-exposure ledger (48.8 % mean top-1) | top-1 clip ≠ #44 in some seeds; mean-vs-peak conflation | A7 confirms same clip 6/6 runs; mean and peak both stated with sources (`A5`, `A7`) | ✅ |
| 17 | Repair census 65.8 % (payoff §2, companion §8) | success budget circular; smoothing hides artifacts; historical directory membership is not the strict flagged set | budget includes independent geometry/velocity terms; no-op and ballistic refusal are external checks. **C4, 2026-08-21:** strict set is 1,606/2,442; the 2,443-row directory adds one feasible control. DFRP's 8 cm primary tier is 644 legacy candidates, all qualification-incomplete. The rate remains bank/operator-specific. | ⚠ corrected membership; root+IK validation panel remains small |
| 18 | Saturation-at-fall (5/29 at ≥98 %) | saturation an artifact of termination thrash, not impact | pre-fall saturation exactly 0.0 in 8/8; onset within 0.6 s of support loss; magnitude ≥ 4/29 in 8/8, 5/29 in 7/8, mean 16.8 % | ⚠️ partly (`effort_sat_at_fall.json`). **The per-actuator identity claim (wrists/waist) is withdrawn 2026-08-19: it is not recoverable from the artifact** — `tools/g1_clip44_gate.py:242` averages over the actuator axis before storage, so no per-actuator array exists. Re-establishing it needs a new rollout. |
| 19 | E-HYG concentration signature | worst-decile regression-to-mean mimics concentration; pruning loses rare coverage | decile defined on comparator; paired pruned deltas. Readout is negative (feasible Δ −0.0101; worst/best −0.0153/−0.0035), so no concentration claim survives | ❌ sealed null (`reports/E_HYG_result.json`) |
| 20 | Cross-bank contrast 0.14 % vs 22.8 % (§1, §6) | two implementations of the method, not one instrument applied twice (μ 0.7 vs 0.6, different MJCF); different *source* corpora, so content — not the retargeter — could drive the gap; BONES-SEED is a `robot_filtered` release, so this is filtered-vs-unfiltered | both share method, 6 cm gap and ½-weight bound; the gap is 160×, orders of magnitude beyond the measured bound sensitivity (flag mass 15.1 → 12.5 % across 0.25–0.75× weight); claim is written as *prevalence is a property of a corpus-and-pipeline pairing*, never as *retargeter X is worse*. **Artifact hygiene closed 2026-08-26:** `reports/feasibility_sonic/hygiene_screen.csv` has 4,950 rows and its sentinel records 0 failures; all registered threshold counts reproduce. Still to do: same-clip cross-implementation agreement, and pre-filter screening only if that bank is obtainable. | ⏳ controlled agreement open; durable artifact closed |
| 21 | Segment curation recovers 61.7 % (§6, companion §8) | min-segment length and bin-eligibility rules chosen after seeing the data; recovered segments may be unlearnable in isolation (the run-up is exactly what the guard band removes) | both rules stated inline as choices with a pre-listed sweep; the claim is a *duration* claim and is labelled as one — no training arm has consumed curated segments, so no survival claim is made; guard-band contrast is within-data (same clips, same screen) | ⏳ open by construction |
| 22 | N3 targeted composition (§8) | added duration rather than ground-family coverage; regression hidden by the E1/E4 arithmetic gate; start-offset phase labels treated as causal boundaries | random16 stays 0.000 while ground16 reaches 0.750/0.750, but adaptive heldout/easy E2 fails and descent unexpectedly improves; retain the preflight stop and the phase-separation miss | ⚠ mixed; no unqualified causal claim (`plan/N3_RESULT.md`) |
| 23 | N7 repair improves deployed tracking (§8.2) | changed reference is merely easier; large geometric edits dominate; one-seed training noise | 2×2 cross: R/raw−K/raw −0.0036, reference-only +0.0233, interaction +0.0200. Eleven over-budget repairs average +0.2305 versus +0.0160 on 78 certified repairs; primary +0.0397 misses +0.05 and coverage fails | ❌ joint rule false; retain bounded easier-target/co-adaptation reading (`plan/N7_RESULT.md`) |
| 24 | Tier800 heldout generalization | `heldout100` overlaps training; startup DR and software environments differ across cells | exact set intersection finds 8/100 overlap; disjoint92 delta −0.0122. Future evaluator freezes per-world conditions and one environment; no “100 unseen” wording | ⚠ sealed survival stands under its harness; unseen/generalization wording corrected (`reports/N7_posthoc_audit.json`) |
| 25 | Segment-native FGAS will improve motion quality | raw failure flux, nonterminal wrap teleports, start-only mask, and survivor-biased quality metrics could mimic or erase benefit | v2 mechanics pass exact-support/truncation and paired-evaluation checks, but the exploratory adaptive distribution is only 0.014 TV from control; §8 reports this as a failed manipulation, not a performance null | ⏳ open by construction; no training claim until a new arm passes its sealed manipulation gate (`plan/SEGMENT_NATIVE_FOLLOWUP_2026-08-20.md`) |
| 26 | DFRP v1 makes repaired clips training-ready (§6, companion §8) | feasibility-only acceptance hides IK artifacts; repaired sidecar can be paired with the wrong motion; feasible controls can be modified | frozen panel keeps residual-feasibility and ≤10 mm IK gates separate, fail-closes source/repair/sidecar/unit-table hashes, and requires 4/4 byte-identical controls. Result: 22/26 flagged exact-ready, 4/4 controls, 36 units, 10,561 legal starts | ✅ implementation gate only; policy benefit explicitly untested (`reports/dfrp_v1_exact_panel/iter1/result.json`) |
